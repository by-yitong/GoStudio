<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark window chrome before Compose paints (design.md paper #17181D).
         The base values/themes.xml stays Material.Light so light-system users
         get a matching light window; this night variant kills the bright flash
         on dark systems, which is the app's default register. -->
    <style name="Theme.ComposeEmptyActivity" parent="android:Theme.Material.NoActionBar" />
</resources>
